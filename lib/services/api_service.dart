//class ApiService {
//  final String baseUrl = "http://localhost/flutter_real_estate_app_api"; // Your base URL

  //Future<List<Property>> fetchProperties() async {
   // try {
     // final response = await http.get(Uri.parse("$baseUrl/get_properties.php"));
      //print('Response status: ${response.statusCode}');
      //print('Response body: ${response.body}');

     // if (response.statusCode == 200) {
        // Parse the JSON response into a list of Property objects
       // List<dynamic> data = jsonDecode(response.body);
        //return data.map((json) => Property.fromJson(json)).toList();
      //} else {
        //throw Exception("Failed to fetch properties. Status code: ${response.statusCode}");
      //}
    //} catch (e) {
     // print("Error fetching properties: $e");
     // throw Exception("Error fetching properties: $e");
    //}
 // }
//}
